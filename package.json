{
  "name": "sleek-chat-interface",
  "version": "0.1.0",
  "private": true,
  "packageManager": "pnpm@10.15.1",
  "scripts": {
    "dev": "pnpm run --filter web dev",
    "build": "pnpm run --filter web build",
    "start": "pnpm run --filter web start",
    "lint": "pnpm run --filter web lint",
    "test": "pnpm run --filter web test",
    "type-check": "pnpm run --filter web type-check"
  },
  "devDependencies": {
    "@types/node": "^20.0.0",
    "typescript": "^5.0.0"
  }
}